<% if (title) { %>
  <h1><%= title %></h1>
<% } else {
  res.redirect('/')
} %>

<%# messages() %>

<main class="error-main">
  <div class="error-container">
    <div class="error-icon">
      <% if ((error && error.status) || status) { %>
        <% const errorCode = (error && error.status) || status || 500; %>
        <% if (errorCode === 404) { %>
          🔍
        <% } else { %>
          ⚠️
        <% } %>
      <% } else { %>
        ⚠️
      <% } %>
    </div>
    
    <div class="error-content">
      <% if ((error && error.status === 404) || status === 404) { %>
        <h2>Page Not Found</h2>
        <p>Sorry, we appear to have lost that page.</p>
      <% } else { %>
        <h2>Oops! Something went wrong</h2>
        <p><%= message || 'Oh no! There was a crash. Maybe try a different route?' %></p>
      <% } %>
      
      <div class="error-actions">
        <a href="/" class="btn btn-primary">Return Home</a>
        <a href="javascript:history.back()" class="btn btn-secondary">Go Back</a>
      </div>
      
      <div class="error-help">
        <h3>What can you do?</h3>
        <ul>
          <li>Check the URL for typos</li>
          <li>Navigate using our menu above</li>
          <li>Return to the homepage</li>
          <li>Contact us if the problem persists</li>
        </ul>
      </div>
    </div>
  </div>
</main>

<style>
.error-main {
  min-height: 70vh;
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 2rem;
  background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
}

.error-container {
  max-width: 600px;
  width: 100%;
  background: white;
  border-radius: 12px;
  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
  padding: 3rem;
  text-align: center;
}

.error-icon {
  font-size: 4rem;
  margin-bottom: 1.5rem;
  opacity: 0.8;
}

.error-content h2 {
  color: #2c3e50;
  margin-bottom: 1rem;
  font-size: 2rem;
  font-weight: 600;
}

.error-content p {
  color: #7f8c8d;
  font-size: 1.1rem;
  line-height: 1.6;
  margin-bottom: 2rem;
}

.error-actions {
  display: flex;
  gap: 1rem;
  justify-content: center;
  flex-wrap: wrap;
  margin-bottom: 2.5rem;
}

.btn {
  padding: 12px 24px;
  border: none;
  border-radius: 6px;
  text-decoration: none;
  font-weight: 500;
  font-size: 1rem;
  transition: all 0.3s ease;
  cursor: pointer;
  display: inline-block;
}

.btn-primary {
  background: #3498db;
  color: white;
}

.btn-primary:hover {
  background: #2980b9;
  transform: translateY(-2px);
}

.btn-secondary {
  background: #ecf0f1;
  color: #2c3e50;
  border: 1px solid #bdc3c7;
}

.btn-secondary:hover {
  background: #d5dbdb;
  transform: translateY(-2px);
}

.error-help {
  text-align: left;
  background: #f8f9fa;
  border-radius: 8px;
  padding: 1.5rem;
  margin-top: 2rem;
}

.error-help h3 {
  color: #2c3e50;
  margin-bottom: 1rem;
  font-size: 1.2rem;
}

.error-help ul {
  margin: 0;
  padding-left: 1.5rem;
  color: #555;
}

.error-help li {
  margin-bottom: 0.5rem;
  line-height: 1.5;
}

@media (max-width: 768px) {
  .error-main {
    padding: 1rem;
    min-height: 60vh;
  }
  
  .error-container {
    padding: 2rem 1.5rem;
  }
  
  .error-content h2 {
    font-size: 1.6rem;
  }
  
  .error-icon {
    font-size: 3rem;
  }
  
  .error-actions {
    flex-direction: column;
  }
  
  .btn {
    width: 100%;
    max-width: 200px;
  }
}
</style>
